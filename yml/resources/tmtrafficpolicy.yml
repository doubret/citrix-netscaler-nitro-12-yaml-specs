scope:                                    traffic-management
key:
  name:                                   name
fields:
  rule:                                   string
  action:                                 tmtrafficaction.name
update:
  - rule
  - action
