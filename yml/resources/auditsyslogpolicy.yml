key:
  name:                                   name
fields:
  rule:                                   string
  action:                                 auditsyslogaction.name
update:
  - rule
  - action
