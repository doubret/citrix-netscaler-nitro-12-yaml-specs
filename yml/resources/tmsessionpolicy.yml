key:
  name:                                   name
fields:
  rule:                                   string
  action:                                 tmsessionaction.name
update:
  - rule
  - action
