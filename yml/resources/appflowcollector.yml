key:
  name:                     name
fields:
  ipaddress:                ip
  port:                     int
  transport:                (ipfix|logstream)
  netprofile:               netprofile.name
update:
  - ipaddress
  - port
  - netprofile
